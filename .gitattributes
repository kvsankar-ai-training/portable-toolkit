# Batch files are the entry point and must keep CRLF wherever they are checked
# out or archived. PowerShell accepts either, but CRLF keeps them consistent.
* text=auto eol=lf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.txt text eol=crlf
